class Experiment:
    def __init__(self, params):
        self.experiment_name = params["experiment_name"]
        self.ds_path = params["ds_path"]
        self.ds_name = params["ds_name"]
        self.name_fn = params["name_fn"]
        self.image_size = params["image_size"]
        self.batch_size = params["batch_size"]
        self.epoch_steps = params["epoch_steps"]
        self.enable_proxy_attention = params["enable_proxy_attention"]
        self.change_subset_attention = params["change_subset_attention"]
        self.validation_split = params["validation_split"]
        self.shuffle_dataset = params["shuffle_dataset"]
        self.num_gpu = params["num_gpu"]
        self.transfer_imagenet = params["transfer_imagenet"]
        self.subset_images = params["subset_images"]
        self.proxy_steps = params["proxy_steps"]
        self.proxy_threshold = params["proxy_threshold"]
        self.pixel_replacement_method = params["pixel_replacement_method"]
        self.model = params["model"]
        self.proxy_steps = params["proxy_steps"]
        self.load_proxy_data = params["load_proxy_data"]
